<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dynamic Dishes</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav class="d-flex justify-content-end px-5 text-dark fs-4">
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a class="text-dark" href="/views/profile.handlebars">Profile</a> |
        <button class="no-button bg-warning rounded border-right" id="logout">Logout</button>
        {{else}}
        <a class="text-dark" href="/views/login.handlebars">Login</a>
        {{/if}}
    </nav>

    <h1 class="mt-1">Dynamic Dishes</h1>
    <h2 class="text-white text-center">Where everyone's a chef!</h2>
     <main class="container container-fluid mt-5 card text-center"> 
        <!-- Render the sub layout -->
        {{{ body }}}
    <!-- Render script for logged in users only -->
     </main>
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
        crossorigin="anonymous"></script>
</body>

</html>